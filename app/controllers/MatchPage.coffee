'use strict'

angular.module('mentrApp')
  .controller 'MatchpageCtrl', ($scope, $timeout) ->
    # Bind controller data
    do ->

    # Attach listeners


    # Initialize controller
    do ->
      steroids.view.navigationBar.show ' '
